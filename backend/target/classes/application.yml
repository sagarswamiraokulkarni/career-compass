spring:
  application:
    name: careercompass
  devtools:
    restart:
      enabled: true
  jpa:
    show-sql: true
  properties:
    hibernate:
      format_sql: true
  #  jpa:
#    hibernate:
#      ddl-auto: create

  datasource:
    url: jdbc:mysql://sql3.freesqldatabase.com:3306/sql3697032
    username: sql3697032
    password: etGymg6EjA

app:
  jwt:
    # Signing key for HS512 algorithm
    # In http://www.allkeysgenerator.com/ you can generate all kinds of keys
    secret: v9y$B&E)H@MbQeThWmZq4t7w!z%C*F-JaNdRfUjXn2r5u8x/A?D(G+KbPeShVkYp
    expiration:
      minutes: 10
  cors:
    allowed-origins: http://localhost:3000

logging:
  level:
    org.springframework.security: DEBUG
    # org.hibernate.SQL: DEBUG
